# Required
OPENAI_API_KEY=

# Optional
OPENAI_MODEL=gpt-4o-mini
PLAYWRIGHT_MCP_SSE_URL=http://localhost:8931/sse

# Provide steps either directly here as JSON or via UI_STEPS_FILE path
# Example JSON steps to search Bing and take a screenshot
UI_STEPS='[
  {"action": "goto", "url": "https://www.bing.com"},
  {"action": "fill", "selector": "input[type=search], input[name=q]", "text": "Microsoft Building 99 address"},
  {"action": "press", "key": "Enter"},
  {"action": "wait_for", "state": "networkidle"},
  {"action": "screenshot", "path": "bing-result.png"}
]'
# UI_STEPS_FILE=/absolute/path/to/steps.json
